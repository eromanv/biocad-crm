meta {
  name: Import plan Excel
  type: http
  seq: 2
}

post {
  url: {{backendUrl}}/api/plan/import
  body: multipartForm
  auth: none
}

body:multipart-form {
  file: @file(../../examples/sample-plan.xlsx)
}
