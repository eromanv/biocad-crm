meta {
  name: Reset plan to seed
  type: http
  seq: 4
}

# Clears chat session history for the request cookie (biocad_chat_sid) after reset.

post {
  url: {{backendUrl}}/api/plan/reset
  body: none
  auth: none
}
