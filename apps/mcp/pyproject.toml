[project]
name = "biocad-mcp"
version = "0.1.0"
description = "Biocad FastMCP plan server"
requires-python = ">=3.12,<3.13"
dependencies = [
  "plan-core",
  "fastapi==0.115.6",
  "uvicorn[standard]==0.34.0",
  "mcp[cli]==1.29.0",
]

[tool.uv]
package = false

[tool.uv.sources]
plan-core = { workspace = true }
