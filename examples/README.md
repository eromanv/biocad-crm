# Examples

- `sample-plan.xlsx` — same columns as Excel import/export:
  `задача`, `описание`, `исполнитель`, `длительность`, `предшественники`

Предшественники — имена задач через запятую. Дат в файле нет: CPM считает их от `project_start`.
